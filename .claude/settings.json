{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit:*)",
            "command": "pnpm build > /tmp/claude-prebuild.log 2>&1 || { echo \"pnpm build FAILED — commit blocked. See /tmp/claude-prebuild.log:\" 1>&2; tail -n 40 /tmp/claude-prebuild.log 1>&2; exit 2; }",
            "timeout": 300,
            "statusMessage": "Running pnpm build before commit..."
          }
        ]
      }
    ]
  }
}
